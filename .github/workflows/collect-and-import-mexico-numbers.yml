name: Collect and import Mexico numbers

on:
  schedule:
    - cron: "0 3 */5 * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect-and-import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install firebase-admin axios --no-save
      - run: node scripts/discover_mexico_phone_sources.js
      - run: node scripts/collect_mexico_phone_sources.js --target=1000 --min=300 --max-minutes=10
      - run: node scripts/pull_firestore_phone_numbers.js || echo "firestore pull skipped"
      - run: node scripts/rebuild_searchable_database.js --min=300 --target=1000
      - run: node scripts/check_data_safety.js --min=300
      - env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        run: node scripts/import_mexico_seed_to_firestore.js --dry-run
      - env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        run: node scripts/import_mexico_seed_to_firestore.js
      - env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
        run: node scripts/export_ios_numbers_from_firestore.js || echo "ios export skipped"
      - run: node scripts/generate_public_stats.js --min=300
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/source_catalog_mexico.json data/collected_mexico_numbers.json data/crowd_signal_mexico_numbers.json data/collector_run_log.json data/public_stats.json data/ios_numbers.json
          if git diff --cached --quiet; then
            echo "No data file changes to commit."
            exit 0
          fi
          git commit -m "chore: update collected mexico numbers"
          git push
