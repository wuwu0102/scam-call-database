# Firestore Admin Helpers

This folder documents safe manual steps for loading shared records into Firestore collection `phone_numbers`.

- `import_mexico_seed_to_firestore.md`: step-by-step import process.
- `firestore_fields.md`: required field map and examples.

These docs keep compatibility with the existing web/PWA and Flutter app read/write flow.
